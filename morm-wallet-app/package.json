{
  "name": "morm-wallet-app",
  "version": "0.0.1",
  "description": "MORM Wallet mobile app (Capacitor) — wraps the wallet web UI + @morm/wallet-core, with native storage.",
  "private": true,
  "scripts": {
    "build:www": "node tools/build_app.mjs",
    "sync": "npm run build:www && npx cap sync",
    "ios": "npm run sync && npx cap open ios",
    "android": "npm run sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6",
    "@capacitor/core": "^6",
    "@capacitor/ios": "^6",
    "@capacitor/preferences": "^6",
    "capacitor-secure-storage-plugin": "^0.10.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6"
  }
}
